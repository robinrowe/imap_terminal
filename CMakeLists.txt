cmake_minimum_required(VERSION 2.8)
project(imap_terminal)
add_executable(imap_terminal main.cpp)
find_package (CURL)
if (CURL_FOUND)
  include_directories(${CURL_INCLUDE_DIRS})
  target_link_libraries (imap_terminal ${CURL_LIBRARIES})
endif (CURL_FOUND)